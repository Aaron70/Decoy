openapi: 3.0.0
info:
  title: Mock Server Example
  version: 1.0.0
paths:
  /user:
    get:
      operationId: GetAllUsers
      responses:
        '200':
          description: 200 response
          content:
            application/json:
              examples:
                decoy:
                  externalValue: decoy:///pagination
